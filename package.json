{
  "name": "asme-receiving-pwa",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "http-server -p 4173 -c-1 .",            // serve repo root; SW needs http
    "pw:install": "playwright install --with-deps",   // one-time browser install
    "test": "playwright test",                        // run tests headless
    "test:headed": "PWDEBUG=1 playwright test"        // debug with UI
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "http-server": "^14.1.1"
  }
}
