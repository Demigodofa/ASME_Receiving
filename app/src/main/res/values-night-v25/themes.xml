<resources>
    <!-- Base application theme using modern Material3. -->
    <style name="Theme.MaterialGuardian" parent="android:Theme.Material.NoActionBar">
        <!-- We use Jetpack Compose for the UI, so these are just fallback values -->
        <item name="android:colorPrimary">#D0BCFF</item>
        <item name="android:colorSecondary">#CCC2DC</item>
        <item name="android:statusBarColor">#D0BCFF</item>
    </style>
</resources>
